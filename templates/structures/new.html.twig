{% extends "base.html.twig" %}

{% block body %}

	<div class="container">
		<h1 class="mt-4">Création d'une structure</h1>

		{{ form_start(form) }}
		<div class="form-group">
			{{form_label(form.adresse)}}
			{{form_widget(form.adresse)}}
			<div class="form-error">
				{{ form_errors(form.adresse) }}
			</div>
		</div>

		<div class="form-group">
			{{form_row (form.submit)}}
		</div>
		{{ form_end(form) }}
	</div>
{% endblock %}